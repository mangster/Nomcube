var camera = {
    x: -canvas.width,
    y: -canvas.height,
    vx: 0,
    vy: 0,
    speed: 400,
    update: function() {
        //this.x = player.hitBox.pos.x;
        //this.y = player.hitBox.pos.y;
        
        //this.x += this.vx / FPS;
        //this.y += this.vy / FPS;
    }
}
